---
const { papers = [] } = Astro.props;
---

{papers.length > 0 && (
  <div class="evidence-links" aria-label="支撑论文">
    <span>依据论文</span>
    <div>
      {papers.map((paper) => (
        <a href={paper.link} title={paper.title}>{paper.title}</a>
      ))}
    </div>
  </div>
)}
